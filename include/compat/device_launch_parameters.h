// Compatibility header: drop-in for <device_launch_parameters.h>
// In real CUDA this provides threadIdx, blockIdx, etc.
// In cuda_sim these are handled by the generated code, so this is empty.
#pragma once
